default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch('RAILS_MAX_THREADS') { 5 } %>
  url: <%= ENV.fetch('DATABASE_URL', Rails.application.credentials.database_url) %>

development:
  <<: *default

test:
  <<: *default
  url: <%= ENV.fetch('TEST_DATABASE_URL') { ENV.fetch('DATABASE_URL', Rails.application.credentials.database_url) } %>

staging:
  <<: *default

production:
  <<: *default
